services:
  api:
    build: ./apps/api
    ports:
      - "8000:8000"
  web:
    build: ./apps/web
    environment:
      AI_API_BASE_URL: http://api:8000
    depends_on:
      - api
    ports:
      - "3000:3000"
